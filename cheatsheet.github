
# HOWTO add items to the repo
git add README

# HOWTO make commits
git commit -m 'first commit'        # will push only added files
git commit -a -m 'improved configs' # will push modified files also

# initially, need to connect with the remote
git remote add origin git@github.com:gbtekkie/playground.git

# after making changes, this is how you push changes to github repo
git push origin master
